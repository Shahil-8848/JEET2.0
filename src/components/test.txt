import { Link, useLocation, Outlet } from 'react-router-dom';
import { Home, PlusCircle, Wallet, ArrowRightLeft, User, LogOut } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

const Layout = () => {
    const { profile, signOut } = useAuth();
    const location = useLocation();

    const isActive = (path) => location.pathname === path;

    return (
        <div className="min-h-screen bg-background text-white flex flex-col">
            {/* Top Header */}
            <header className="bg-surface border-b border-gray-800 px-6 py-4 flex items-center justify-between">
                <div className="flex items-center gap-3">
                    <div className="w-10 h-10 bg-primary rounded-full flex items-center justify-center font-bold text-black text-xl">
                        <img
                            src="https://img.freepik.com/premium-vector/dragon-esport-logo_91719-96.jpg"
                            alt="Logo"
                            className="w-full h-full rounded-full object-contain"
                        />
                    </div>
                    <h1 className="text-2xl font-bold tracking-wider">JEET 2.0</h1>
                </div>

                {/* User Profile - Desktop */}
                <div className="hidden md:flex items-center gap-4">
                    <div className="flex items-center gap-3 bg-gray-800/50 rounded-full px-4 py-2">
                        <div className="w-8 h-8 bg-gray-700 rounded-full flex items-center justify-center text-sm">
                            {profile?.full_name?.[0] || 'U'}
                        </div>
                        <div>
                            <p className="font-medium text-sm">{profile?.full_name || 'User'}</p>
                            <p className="text-xs text-primary">Rs.{profile?.balance || 0}</p>
                        </div>
                    </div>
                    <button
                        onClick={signOut}
                        className="p-2 hover:bg-gray-800 rounded-lg transition-colors text-error"
                        title="Logout"
                    >
                        <LogOut size={20} />
                    </button>
                </div>

                {/* User Profile - Mobile */}
                <div className="md:hidden flex items-center gap-2">
                    <div className="w-8 h-8 bg-gray-700 rounded-full flex items-center justify-center text-sm">
                        {profile?.full_name?.[0] || 'U'}
                    </div>
                    <p className="text-sm text-primary">Rs.{profile?.balance || 0}</p>
                </div>
            </header>

            {/* Main Content */}
            <main className="flex-1 overflow-y-auto pb-24">
                <Outlet />
            </main>

            {/* Bottom Navigation Bar - Figma Style */}
            <nav className="fixed bottom-6 left-1/2 -translate-x-1/2 z-50 w-auto max-w-[95vw]">
                <div className="relative">
                    {/* Animated Rotating Neon Border - Creates the traveling light effect */}
                    {/* <div className="absolute inset-0 rounded-full p-[2px]">
                        <div
                            className="absolute inset-0 rounded-full opacity-75 animate-spin"
                            style={{
                                background: 'conic-gradient(from 0deg, #f97316, #ec4899, #8b5cf6, #3b82f6, #10b981, #f97316)',
                                animationDuration: '3s'
                            }}
                        ></div>
                    </div> */}

                    {/* Outer Glow - Pulsing effect */}
                    <div className="absolute -inset-2 rounded-full bg-gradient-to-r from-orange-500/20 via-pink-500/20 via-purple-500/20 to-blue-500/20 blur-xl animate-pulse"></div>

                    {/* Main Navigation Container */}
                    <div className="relative bg-gradient-to-br from-gray-900 via-gray-900 to-gray-800 backdrop-blur-xl rounded-full shadow-2xl px-3 py-3 border-[3px] border-transparent">
                        {/* Inner glow effect */}
                        <div className="absolute inset-0 rounded-full bg-gradient-to-r from-orange-500/10 via-pink-500/10 to-purple-500/10"></div>

                        <div className="relative flex items-center gap-2 flex-wrap sm:flex-nowrap justify-center">
                            {/* Left Section */}
                            <button
                                className={`flex items-center gap-2 px-4 py-2.5 rounded-full transition-all duration-300 ${isActive('/')
                                    ? 'bg-gradient-to-r from-orange-500/30 to-pink-500/30 text-orange-400 shadow-lg shadow-orange-500/30 border border-orange-500/50'
                                    : 'hover:bg-gray-800/70 text-gray-400 hover:text-white'
                                    }`}
                                title="Dashboard"
                            >
                                <Home size={20} />
                                <span className="hidden sm:inline text-sm font-medium">Home</span>
                            </button>

                            <button
                                className={`flex items-center gap-2 px-4 py-2.5 rounded-full transition-all duration-300 ${isActive('/profile')
                                    ? 'bg-gradient-to-r from-blue-500/30 to-cyan-500/30 text-blue-400 shadow-lg shadow-blue-500/30 border border-blue-500/50'
                                    : 'hover:bg-gray-800/70 text-gray-400 hover:text-white'
                                    }`}
                                title="Profile"
                            >
                                <User size={20} />
                                <span className="hidden sm:inline text-sm font-medium">Profile</span>
                            </button>

                            {/* Separator with Glow */}
                            <div className="relative mx-1 hidden sm:block">
                                <div className="w-px h-8 bg-gradient-to-b from-transparent via-gray-600 to-transparent"></div>
                                <div className="absolute inset-0 w-px bg-gradient-to-b from-transparent via-orange-500/50 to-transparent blur-sm"></div>
                            </div>

                            {/* Center - Create Room (Primary Action) */}
                            <button className="relative group flex items-center gap-2 px-6 py-2.5 rounded-full transition-all font-semibold overflow-hidden">
                                {/* Animated Background Gradient */}
                                <div className="absolute inset-0 bg-gradient-to-r from-yellow-400 via-orange-500 to-yellow-400 group-hover:from-orange-500 group-hover:via-yellow-400 group-hover:to-orange-500 transition-all duration-500"></div>

                                {/* Shimmer Effect on Hover */}
                                <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/40 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000 ease-in-out"></div>

                                {/* Content */}
                                <div className="relative flex items-center gap-2 text-black z-10">
                                    <PlusCircle size={22} strokeWidth={2.5} className="drop-shadow-lg" />
                                    <span className="text-sm font-bold drop-shadow-lg">Create Room</span>
                                </div>

                                {/* Pulsing Outer Glow */}
                                <div className="absolute -inset-1 rounded-full bg-gradient-to-r from-yellow-400 to-orange-500 opacity-50 group-hover:opacity-75 blur-md animate-pulse"></div>
                            </button>

                            {/* Separator with Glow */}
                            <div className="relative mx-1 hidden sm:block">
                                <div className="w-px h-8 bg-gradient-to-b from-transparent via-gray-600 to-transparent"></div>
                                <div className="absolute inset-0 w-px bg-gradient-to-b from-transparent via-purple-500/50 to-transparent blur-sm"></div>
                            </div>

                            {/* Right Section */}
                            <button
                                className="flex items-center gap-2 px-4 py-2.5 rounded-full hover:bg-gradient-to-r hover:from-green-500/30 hover:to-emerald-500/30 text-gray-400 hover:text-green-400 transition-all duration-300 hover:border hover:border-green-500/50 hover:shadow-lg hover:shadow-green-500/30"
                                title="Top Up Wallet"
                            >
                                <Wallet size={20} />
                                <span className="hidden sm:inline text-sm font-medium">Wallet</span>
                            </button>

                            <button
                                className="flex items-center gap-2 px-4 py-2.5 rounded-full hover:bg-gradient-to-r hover:from-purple-500/30 hover:to-pink-500/30 text-gray-400 hover:text-purple-400 transition-all duration-300 hover:border hover:border-purple-500/50 hover:shadow-lg hover:shadow-purple-500/30"
                                title="Transfer Funds"
                            >
                                <ArrowRightLeft size={20} />
                                <span className="hidden sm:inline text-sm font-medium">Transfer</span>
                            </button>
                        </div>
                    </div>

                    {/* Bottom Reflection Glow */}
                    <div className="absolute bottom-0 left-1/2 -translate-x-1/2 translate-y-full w-3/4 h-8 bg-gradient-to-r from-transparent via-orange-500/30 to-transparent blur-2xl"></div>
                </div>
            </nav>
        </div>
    );
};

export default Layout;